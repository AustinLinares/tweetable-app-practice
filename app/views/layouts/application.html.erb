<!DOCTYPE html>
<html>
  <head>
    <title>TweetableAppPractice</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <header>
    <%=link_to "Welcome, #{current_user.username}" if user_signed_in? %>
    <%=link_to "Sign in", new_user_session_path unless user_signed_in? %>
    <%=link_to "Sign up", new_user_registration_path unless user_signed_in? %>
    <%=link_to "Sign out", destroy_user_session_path, data: { turbo_method: :delete } if user_signed_in? %>
    
  </header>
  <body>
    <%=notice%>
    <%=alert%>
    <%= yield %>
  </body>
</html>
